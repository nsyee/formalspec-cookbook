\* module: MCApproval
\* expect: counterexample
\* 活性が「進行仮定なしでは成り立たない」ことの確認。安全性の仕様 Spec は上長が
\* 何もしない振る舞い（ずっと Pending のまま）も許すので、TLC は反例（ループを
\* 含むトレース）を返さなければならない。FairSpec との差が進行仮定そのものである。

SPECIFICATION Spec

CONSTANTS
    User = {u1, u2}
    Department = {d1, d2}
    Request = {r1}
    Revision = {v1, v2}
    Nil = Nil

PROPERTY PendingIsEventuallyDecided
