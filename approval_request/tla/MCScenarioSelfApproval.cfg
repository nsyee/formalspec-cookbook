\* module: MCScenarios
\* expect: counterexample
\* 自己決裁が実際に起こり得ること（spec.md §4 D）。反例＝そのシナリオのトレース。

SPECIFICATION SSpec

CONSTANTS
    User = {u1, u2, u3}
    Department = {d1, d2}
    Request = {r1}
    Revision = {v1, v2}
    Nil = Nil
    MaxLog = 3

CONSTRAINT LogIsBounded

INVARIANT SelfApprovalIsImpossible
