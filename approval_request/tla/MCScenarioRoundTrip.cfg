\* module: MCScenarios
\* expect: counterexample
\* 提出 → 差し戻し → 編集 → 再提出 → 承認 の往復が起こり得ること。

SPECIFICATION SSpec

CONSTANTS
    User = {u1, u2, u3}
    Department = {d1, d2}
    Request = {r1}
    Revision = {v1, v2}
    Nil = Nil
    MaxLog = 5

CONSTRAINT LogIsBounded

INVARIANT RoundTripIsImpossible
