\* module: MCApproval
\* expect: ok
\* アクション性質: 「どの遷移が許されるか」についての制約（spec.md §2, §5 P1/P2）。
\* 対称性による状態空間の商は不変条件の検査にしか使えないため、ここでは指定しない。

SPECIFICATION Spec

CONSTANTS
    User = {u1, u2, u3}
    Department = {d1, d2}
    Request = {r1}
    Revision = {v1, v2}
    Nil = Nil

INVARIANT TypeOK

PROPERTIES
    TerminalRequestsAreFrozen
    StatusFollowsStateMachine
    TerminalDecidedByTargetManager
    ContentChangesNeedAuthorizedEditor
