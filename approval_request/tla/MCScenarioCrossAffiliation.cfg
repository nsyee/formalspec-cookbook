\* module: MCScenarios
\* expect: counterexample
\* 兼務ユーザーが申請先部署では一般メンバーであり、他の上長に承認されるシナリオ（P1 の背景）。

SPECIFICATION SSpec

CONSTANTS
    User = {u1, u2, u3}
    Department = {d1, d2}
    Request = {r1}
    Revision = {v1, v2}
    Nil = Nil
    MaxLog = 3

CONSTRAINT LogIsBounded

INVARIANT CrossAffiliatedApprovalIsImpossible
